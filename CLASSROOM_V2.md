# 核心课堂 V2：自适应学习工作台

## 1. 产品判断

当前课堂页完成了 MVP 最基本的链路：生成课程内容、展示讲解、提供一道练习、允许用户向 AI 追问。但它仍然是“AI 生成的长文章 + 聊天框”，用户可以从头滚到尾，却没有任何机制判断他是否理解、哪里卡住、下一步应该做什么。

课堂 V2 不再把“内容生成量”当成核心价值，而把下面这条闭环作为产品核心：

> 明确目标 → 建立心智模型 → 理解关键机制 → 主动应用 → 获取诊断 → 调整路径 → 形成掌握证据

页面的核心问题也随之改变：

- 不再问“这一节要展示哪些内容”，而是问“此刻最值得学习者做什么”。
- 不再用“读完、点完成”代表学会，而是积累可解释的掌握证据。
- 不再把 Agent 放在独立聊天框里，而是让它持续观察学习阶段和行为证据。

## 2. 页面信息架构

### 2.1 左侧：课程地图

作用是回答“我在哪里”，只承担跨章节、小节的定位，不承担当前学习任务。

- 章节与小节目录
- 当前小节高亮
- 已完成 / 当前 / 待解锁状态
- 离开当前小节前保留本节学习状态

### 2.2 中间：聚焦式学习画布

中间区域不再同时堆叠思维导图、讲解、示例和练习，而是按一次学习决策拆成四个阶段：

1. **定位**：看到本节目标、知识关系和先后顺序。
2. **理解**：阅读核心讲解，并用一个完整示例建立机制理解。
3. **应用**：主动作答；回答前 Agent 只给提示，回答后才进行诊断。
4. **收束**：查看掌握证据、表达主观信心、决定完成或回到薄弱阶段。

阶段可以返回，但推荐路径始终只有一个主动作，避免“满屏入口但不知道该做什么”。

### 2.3 右侧：Agent 教练台

右侧首先展示的是学习状态，而不是聊天记录：

- 当前观察：学习者正处于什么阶段
- 掌握证据：已经验证了哪些能力
- 下一步建议：此刻最值得做的动作
- 快捷干预：当前阶段可用的 Agent 能力
- 对话：作为补充交互保留在下半区

## 3. 学习状态模型

每个小节维护一份 `LearningSession`：

```ts
type LearningPhase = "orient" | "understand" | "practice" | "reflect";

type LearningSession = {
  sectionId: string;
  phase: LearningPhase;
  visitedPhases: LearningPhase[];
  attempts: Array<{
    questionId: string;
    answer: string;
    correct: boolean;
    misconception?: string;
    createdAt: string;
  }>;
  confidence?: "uncertain" | "partial" | "ready";
  mastery: {
    mentalModel: "unknown" | "observed";
    discrimination: "unknown" | "weak" | "observed";
    transfer: "unknown" | "weak" | "observed";
  };
  nextAction: string;
  updatedAt: string;
};
```

课堂 V2 首版可以先在前端会话内维护阶段、作答和信心；下一版再持久化到服务端，作为复习计划和跨节个性化的输入。

## 4. Agent 体系

不建议把每个 UI 模块都做成一个会聊天的 Agent。更稳定的结构是一个编排器维护学习状态，专业 Agent 只完成边界清晰的任务。

### 4.1 Learning Orchestrator（学习编排器）

- 输入：当前小节、阶段、最近行为、作答结果、历史掌握度
- 输出：下一学习动作、需要调用的专业 Agent、界面应该聚焦的学习块
- 原则：规则优先、模型辅助；关键状态转换不完全依赖 LLM

### 4.2 Content Agent（内容设计）

- 按“概念单元”生成，而不是一次生成整篇文章
- 输出目标、心智模型、解释、例子、检查点
- 对每个事实保留来源或可验证依据
- 当前项目中的 `course-content` Agent 可以演进承担此角色

### 4.3 Coach Agent（苏格拉底式助教）

- 感知当前阶段、作答与信心，而非只看到聊天历史
- 作答前优先提示和追问，不直接泄露答案
- 作答错误后识别误区，给一个最小解释，再用检查问题确认
- 作答正确后不只表扬，而是要求解释理由或进行轻量迁移
- 当前项目中的 `tutor` Agent 可以演进承担此角色

### 4.4 Evaluator Agent（评估与诊断）

- 评估选择题、开放回答、解释题和代码任务
- 输出结构化结果：得分、证据、误区标签、反馈、建议动作
- 不能只返回“正确/错误”

### 4.5 Memory Agent（学习记忆）

- 将一次会话中的证据压缩为稳定画像
- 更新概念掌握度、遗忘风险、常见误区、有效解释偏好
- 为计划页、复习页和下一节内容提供上下文
- 不保存无关聊天内容，画像必须允许用户查看和修正

## 5. 关键交互规则

### 5.1 进入小节

1. 加载已保存的 `LearningSession`。
2. 如果是首次进入，从“定位”开始。
3. 如果是继续学习，恢复上次阶段并显示一句恢复提示。
4. Content Agent 只补齐缺失的学习块，不默认整节重新生成。

### 5.2 回答练习

1. 回答前点击“给我提示”时，Coach Agent 只能给分级提示。
2. 提交后记录答案和耗时。
3. Evaluator 输出误区标签与掌握证据。
4. 编排器选择：进入收束、给一道变式题、退回讲解中的特定片段。

### 5.3 标记完成

“标记完成”不应始终可用：

- 至少完成一次主动应用后才推荐完成。
- 回答错误时仍可完成，但界面明确记录“带薄弱点完成”，并自动进入复习队列。
- 用户主观信心与客观证据冲突时，Agent 要指出冲突，而不是替用户做决定。

## 6. 掌握度不是一个神秘百分比

首版展示三类可理解的证据：

- **心智模型**：是否看过并能复述知识关系
- **辨析能力**：是否能在题目中识别正确机制
- **迁移能力**：是否能处理变式或解释为什么

百分比只用于快速扫视，旁边必须始终展示证据来源。后续可采用 BKT/IRT 等模型，但前端仍应把结果翻译成人能理解的语言。

## 7. 数据与 API 演进

建议增加：

- `GET /projects/:projectId/sections/:sectionId/session`
- `PATCH /projects/:projectId/sections/:sectionId/session`
- `POST /projects/:projectId/sections/:sectionId/evaluate`
- `POST /projects/:projectId/sections/:sectionId/coach`
- `POST /projects/:projectId/sections/:sectionId/next-action`

Coach 请求至少携带：

```json
{
  "message": "为什么不是 B？",
  "learningContext": {
    "phase": "practice",
    "attempt": "incorrect",
    "confidence": "partial"
  }
}
```

## 8. 本轮实现边界

本轮先交付可运行的课堂 V2 交互骨架：

- 四阶段学习航线
- 聚焦式内容切换
- 掌握证据与下一步建议
- 练习结果驱动 Agent 状态
- Coach Agent 接收结构化学习上下文

服务端持久化、开放题评估、变式题生成和长期记忆作为下一阶段能力。

## 9. 用户文案原则

Agent、Orchestrator、Evaluator、Evidence 等术语只用于代码和内部设计文档，不直接出现在学习界面。

面向用户的文案遵循：

- 说用户正在做什么，不解释系统正在运行什么。
- 使用“接下来”“试一试”“回顾一下”等日常表达。
- 不使用“实时观察、智能编排、证据覆盖、心智模型、迁移能力”等系统术语。
- 模型名称、Agent 名称和生成过程默认隐藏，只在设置、资料详情或问题排查时展示。
- 反馈像老师的具体建议，不使用“赋能、洞察、个性化路径”等宣传语言。

## 10. 资料如何进入课堂

大纲生成时保存课程使用过的资料，并把相关资料分配到具体小节。整理课堂内容时：

1. 优先读取当前小节已经分配的资料；
2. 资料少于基本需要，或用户主动检查更新时，围绕当前小节进行针对性搜索；
3. 将新增资料合并回课程资料库，并保存到小节；
4. 讲解、示例、练习和学习助手使用同一批资料；
5. 页面显示资料入口，但不在正文中堆叠引用编号；
6. 旧课程继续正常显示，用户检查更新后再补齐资料关系。
