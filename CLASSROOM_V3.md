# 核心课堂 V3：学习 → 做题 → 回顾

## 1. 产品判断

V2 的四阶段（定位 / 理解 / 应用 / 回顾）骨架合理，但每一阶段"装了什么"太虚：

| 环节 | V2 实际内容 | 问题 |
|---|---|---|
| 定位 | 思维导图 + 少量 visualElements | 只是预览，不承担教学 |
| 理解 | scenes 幕 + explanation 段落 | 静态图文，没有可操作的演示 |
| 应用 | 1 道 4 选 1 选择题 | 题型单一、数量为 1，无法判断掌握 |
| 回顾 | 反思文本框 + 信心选择 | 没有真实复习机制 |

本质就是三条实线：**学习（讲透）→ 做题（验证）→ 回顾（巩固）**。V3 把四阶段精简为三段，每段做实。

## 2. 铁律：学科中立（所有 prompt 与 schema 必须遵守）

课程不限于数学和编程，可能涵盖历史、语言、生物、经济、法律等任意学科。

- **prompt 中禁止用学科绑定的例子**（如"基本求导公式表""积分表""泰勒公式"）作为必须照做的模板；示例只能用来说明"某一类内容该怎么写"，并明确标注"根据当前学科替换对应内容"。
- **题型分布由小节内容自主决定**：prompt 只约束"题型必须摊开、不连续出现两个同型"，具体用哪几种由学科内容推导，不预设"数学课必须有计算题"。
- **交互演示按"通用类型"定义**，渲染器只认类型、不认学科：
  - `slider`（可拖参数变化，数学/物理/经济均可）
  - `step-animation`（步骤推进动画，推导、流程、时间线、生物过程均可）
  - `compare`（对照/切换，概念对比、方案对比）
  - `transform`（图形/结构变换，几何、结构图）
  - LLM 为演示块选择类型时，优先选择当前内容最自然的类型；不搞学科→类型硬编码。
- **术语**：面向用户文案不使用系统术语（同 V2 §9）。

## 3. 三段式设计

### 3.1 学习（讲透 + 看得见摸得着）

- 开头：目标 + 知识地图压缩成"本节将学到什么"一行卡（吸收 V2 的 orient）。
- 主体：讲解段落 + `visualElements`（图文并茂）+ `interactiveDemos`（可操作演示）+ scenes"先判断后讲解"机制保留。
- 每个 scenes 幕挂 0–1 个演示块，不强制每幕都有。

### 3.2 做题（题库，题型摊开）

- 每节 5–10 题，按小节内容混合分布，**不连续出现两个同题型**。
- 题型库（通用，均不绑定学科）：
  - 单选 / 多选 / 判断（任何学科）
  - 填空（术语、公式、史实、词汇，关键词匹配自动判分）
  - 匹配 / 连线（概念与定义、事件与年代、对象与属性）
  - 排序（流程步骤、时间线）
  - 计算 / 代码题（结果判定；仅当本节内容确实涉及计算或代码时使用）
  - 简答 / 解释（LLM evaluator 判分，最能验证真掌握）
- 答错 → 同一知识点出变式题再练，直到验证掌握；变式题与原题题型不同。

### 3.3 回顾（SM-2 简化间隔递增）

- 做题结果 → 每知识点掌握度（正确率 + 自信度）。
- 排期：答对 1 → 3 → 7 → 15 → 30 天递增；答错或"不确定"重置回 1 天。
- `learning-planner` agent 做实：输出今日复习队列；前端计划页展示到期小节 + 复习入口。

## 4. 数据模型变化（暂不改代码，先约定）

- `LessonContent`：新增 `interactiveDemos?: InteractiveDemo[]`；`exercise`（单题）升级为 `exercises: ExerciseItem[]`（多题、多题型，每项含 `type`、`knowledgePoint`、变式关联）。
- `LessonProgress`：知识点复习状态扩展 `nextReviewAt`、间隔、重复次数（已有雏形）。
- 新增 `ExerciseItem` 联合类型：single-choice / multi-choice / true-false / fill-blank / matching / ordering / calculation / explanation。

## 5. Agent 变化

| Agent | 变化 |
|---|---|
| `course-content` | 输出演示块 + 题库；prompt 遵循学科中立铁律 |
| `exercise` | 从"出一批选择题"改为"按题型摊开出题" + 变式生成 |
| 新增/实化 `evaluator` | 简答/解释题结构化判分（得分、误区标签、反馈） |
| `learning-planner` | stub → 真实排期：SM-2 简化 + 今日复习队列 |

## 6. 前端变化

- 阶段 4 → 3（学习/做题/回顾）。
- 练习组件支持多题型渲染 + 变式重练。
- 计划页新增复习队列（到期小节、复习入口）。
- 新增 `InteractiveDemoRenderer`：按类型渲染演示块（首期实现 `slider` 函数式绘图 + `step-animation`）。

## 7. 落地顺序（每步独立上线）

1. **题库**：`exercises[]` 数据模型 + course-content/exercise prompt（题型摊开、学科中立）+ 前端多题型渲染 + 变式重练。
2. **复习闭环**：知识点掌握度记录 + SM-2 排期 + learning-planner 做实 + 计划页复习队列。
3. **交互演示**：InteractiveDemo schema + 渲染器（slider / step-animation 起步）。

## 8. 实现边界

首版不做：多选/匹配/排序等复杂题型的完整判定（先单选/判断/填空/计算 + 简答 evaluator）；完整 SM-2 算法（用简化间隔表）；复杂几何/3D 演示（渲染器留扩展位）。
